/**
 * @file modbus_config.h
 * @brief Modbus配置参数
 * @author Claude
 * @date 2026-03-17
 */

#ifndef MODBUS_CONFIG_H
#define MODBUS_CONFIG_H

#ifdef __cplusplus
extern "C" {
#endif

/* ============================================================================
 * 从站配置
 * ============================================================================ */

/* 默认从站地址 */
#define MB_SLAVE_ID_DEFAULT         1

/* ============================================================================
 * 缓冲区配置
 * ============================================================================ */

/* 收发缓冲区大小 */
#define MB_BUFFER_SIZE              256

/* ============================================================================
 * 数据区大小配置
 * ============================================================================ */

/* 线圈区大小（按位计） */
#define MB_COILS_SIZE               1024

/* 离散输入区大小（按位计） */
#define MB_DISCRETE_INPUTS_SIZE     1024

/* 保持寄存器区大小（按16位计） */
#define MB_HOLDING_REGISTERS_SIZE   512

/* 输入寄存器区大小（按16位计） */
#define MB_INPUT_REGISTERS_SIZE     512

/* ============================================================================
 * 定时器配置
 * ============================================================================ */

/*
 * T3.5超时时间（毫秒）
 * Modbus RTU要求帧间隔至少3.5个字符时间
 *
 * 波特率     3.5字符时间（11位/字符）
 * 9600       ~4.0ms
 * 19200      ~2.0ms
 * 38400      ~1.0ms
 * 57600+     建议固定1.75ms
 */
#define MB_T35_TIMEOUT_MS           10      /* 10ms，适用于9600~19200bps */

/*
 * T1.5超时时间（毫秒）
 * 帧内字符间隔最大1.5个字符时间
 * 超过此时间表示帧错误
 */
#define MB_T15_TIMEOUT_MS           5       /* 5ms */

/* ============================================================================
 * 串口配置
 * ============================================================================ */

/* 默认波特率 */
#define MB_UART_BAUDRATE_DEFAULT    9600

/* 默认校验方式：0=无校验, 1=奇校验, 2=偶校验 */
#define MB_UART_PARITY_DEFAULT      2       /* 偶校验 */

/* ============================================================================
 * 功能开关
 * ============================================================================ */

/* 是否支持广播地址 */
#define MB_SUPPORT_BROADCAST        1

/* 是否启用统计功能 */
#define MB_ENABLE_STATS             1

/* 是否启用调试输出 */
#define MB_DEBUG_ENABLE             0

/* ============================================================================
 * 调试配置
 * ============================================================================ */

#if MB_DEBUG_ENABLE
#include <stdio.h>
#define MB_DEBUG_PRINT(fmt, ...)    printf("[MODBUS] " fmt "\n", ##__VA_ARGS__)
#else
#define MB_DEBUG_PRINT(fmt, ...)    ((void)0)
#endif

#ifdef __cplusplus
}
#endif

#endif /* MODBUS_CONFIG_H */
